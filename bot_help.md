```
help			               # вывести справку
list [D]			           # вывести последние D диалогов, по умолчанию 10
print D [M]                    # вывести последние M сообщений из диалога c номером телефона D (без +7), по умолчанию 10
profiles                       # вывести параметры всех профилей
profile                        # вывести название и параметры текущего профиля
profile name number password   # ввести параметры профиля name
select profile                 # выбрать профиль profile
remove profile                 # удалить профиль с именем profile
captcha key                    # ввести капчу key для текущего профиля
captcha                        # получить капчу для текущего профиля
```